<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Imagen circular -->
    <style name="CircularImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>


    <!-- TextViews -->

        <!-- Título principal -->
        <style name="TextViewTitles" parent="Widget.AppCompat.TextView">
            <item name="android:textColor">@color/primary </item>
            <item name="android:textSize">30sp</item>
            <item name="android:layout_marginStart">30dp</item>
            <item name="android:layout_marginTop">20dp</item>
            <item name="android:textStyle">bold</item>
        </style>

        <!-- Texto de color primario -->
        <style name="TextViewPrimaryColor" parent="Widget.AppCompat.TextView">
            <item name="android:textColor">@color/primary </item>
        </style>

        <!-- Subtítulos de experiencias -->
        <style name="TextViewExperiences" parent="Widget.AppCompat.TextView">
            <item name="android:textColor">@color/primaryVariant</item>
            <item name="android:textSize">20sp</item>
            <item name="android:layout_marginTop">4dp</item>
            <item name="android:textStyle">bold</item>
        </style>

    <!-- Botón con borde azul -->
    <style name="ButtonsBorder" parent="Widget.Material3.Button">
        <item name="strokeColor">@color/primary </item>
        <item name="strokeWidth">2dp</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="android:textColor">@color/primary </item>
    </style>
</resources>